1. To use the program please follow the instructions on the menu 
2. in order to add another opperation  please create another case in the switch statment and create the function
3. in order to add hex numbers please add two numbers to the txt file

the way this program opperates is that it takes two number stores them in strings and converts those strings into decimal values.
the decimal values are then added and the program uses the hex function to display the number in hexadecimal.
